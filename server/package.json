{
  "name": "server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file=../.env --hot index.ts",
    "build": "bun tsc"
  },
  "dependencies": {
    "hono": "^4.7.1",
    "common": "workspace:common"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}

{
  "name": "server",
  "scripts": {
    "dev": "bun run --env-file=../.env --hot index.ts",
    "build": "bun tsc",
    "db": "bun run  --env-file=../.env prisma db push",
    "prepare": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "prisma": "^6.3.1",
    "typescript": "^5.7.3"
  }
}

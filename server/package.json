{
  "name": "server",
  "scripts": {
    "dev": "bun run --env-file=../.env --hot index.ts",
    "build": "bun tsc"
  },
  "dependencies": {
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}

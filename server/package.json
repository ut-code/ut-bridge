{
  "name": "server",
  "scripts": {
    "dev": "bun run --env-file=../.env --hot index.ts",
    "build": "bun tsc",
    "db": "bun run  --env-file=../.env prisma db push",
    "prepare": "prisma generate"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@prisma/client": "^6.3.1",
    "hono": "^4.7.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.2",
    "prisma": "^6.3.1",
    "typescript": "^5.7.3"
  }
}
